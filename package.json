{
  "name": "arsip-surat-worker",
  "version": "1.0.0",
  "description": "Backend Arsip Surat St. Agatha — Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.60.0"
  }
}
